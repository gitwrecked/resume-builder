// user service unit test ===================
// angular unit test to validate service logic and methods
describe('user.svc', function() {
    var userSvc, httpBackend;
    beforeEach(module('resumebuilder.app'));

    it('test', function() {
        console.log('user.svc: add a test!');
    });
});