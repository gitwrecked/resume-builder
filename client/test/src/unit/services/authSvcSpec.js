// user service unit test ===================
// angular unit test to validate service logic and methods
describe('authSvc', function() {
    var authSvc, httpBackend;
    beforeEach(module('rbApp'));

    it('test', function() {
        console.log('authSvc: add a test!');
    });
});