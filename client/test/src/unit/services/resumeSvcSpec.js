// resume service unit test ===================
// angular unit test to validate service logic and methods
describe('resume.svc', function() {
    var registerSvc, httpBackend;
    beforeEach(module('resumebuilder.app'));

    it('test', function() {
        console.log('resume.svc: add a test!');
    });
});