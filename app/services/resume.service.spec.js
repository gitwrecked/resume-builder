// resume service unit test ===================
// angular unit test to validate service logic and methods
describe('resume.service', function() {
    var registerService, httpBackend;
    beforeEach(module('resume.module'));
    
    it('test', function() {
        console.log('resume.service: add a test!');
    });
});