// user service unit test ===================
// angular unit test to validate service logic and methods
describe('user.service', function() {
    var userService, httpBackend;
    beforeEach(module('user.module'));

    it('test', function() {
        console.log('user.service: add a test!');
    });
});